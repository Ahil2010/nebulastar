<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="style.css">
    <title>Admin Login</title>
</head>
<body>

    <div class="container" id="login-container">
        <form class="login-form">
            <h1>Admin Login</h1>
            <label for="username">Username:</label>
            <input type="text" id="username" name="username" required>
            <label for="password">Password:</label>
            <input type="password" id="password" name="password" required>
            <button type="button" onclick="checkLogin()">Login</button>
        </form>
    </div>

    <div class="container" id="video-container" style="display: none;">
        <video id="hackerVideo" src="video.mp4" autoplay unmuted onended="redirectToWelcomePage()"></video>
    </div>

    <script>
        function checkLogin() {
            var username = document.getElementById('username').value;
            var password = document.getElementById('password').value;

            if (username === 'ahil' && password === 'ahilisbest') {
                // Hide login container
                document.getElementById('login-container').style.display = 'none';

                // Show video container
                var videoContainer = document.getElementById('video-container');
                videoContainer.style.display = 'block';

                // Play the video
                var video = document.getElementById('hackerVideo');
                video.play();
            } else {
                alert('Invalid username or password. Try again.');
            }
        }

        function redirectToWelcomePage() {
            // Redirect to another HTML page
            window.location.href = 'adminpage';
        }
    </script>

    <style>
    html *
{
   font-family: Courier New !important;
}
        body {
            background-color: #000; /* Set background color to black */
            color: #fff;
            font-family: 'Courier New', monospace;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
        }

        .container {
            background-color: #1a1a1a;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(255, 255, 255, 0.2);
        }

        .login-form {
            text-align: center;
        }

        label {
            display: block;
            margin-top: 10px;
        }

        input {
            width: 100%;
            padding: 8px;
            margin-top: 5px;
            margin-bottom: 15px;
            box-sizing: border-box;
            border: 1px solid #555;
            border-radius: 4px;
        }

        button {
            background-color: #4caf50;
            color: #fff;
            padding: 10px 15px;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

        button:hover {
            background-color: #45a049;
        }

    </style>

</body>
</html>
